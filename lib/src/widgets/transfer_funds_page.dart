import 'package:flutter/material.dart';

class TransferFundsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("TF page"),
    );
  }
}